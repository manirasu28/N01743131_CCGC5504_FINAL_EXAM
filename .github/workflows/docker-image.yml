name: Build and Push Docker image

on:
  workflow_dispatch:

  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

env:
  REGISTRY: docker.io
  IMAGE_TAG: ${{ github.run_number }}

jobs:
  build-and-push:
    runs-on: ubuntu-latest
    permissions:
      contents: write

    steps:
    - name: Checkout repository
      uses: actions/checkout@v4

    - name: Set up Docker Buildx
      uses: docker/setup-buildx-action@v3

    - name: Log in to Docker Hub
      uses: docker/login-action@v3
      with:
        registry: ${{ env.REGISTRY }}
        username: ramaswamywari63
        password: ${{ secrets.DOCKERHUB_TOKEN }}

    - name: Build and push Docker image
      uses: docker/build-push-action@v5
      with:
        context: ./webapp
        push: true
        tags: ${{ env.REGISTRY }}/ramaswamywari63/final-exam:${{ env.IMAGE_TAG }}
        cache-from: type=gha
        cache-to: type=gha,mode=max

    - name: Update Helm chart
      run: |
        # Update the values.yaml file
        cd helm/nginx-app
        yq eval ".image.tag = \"${{ env.IMAGE_TAG }}\"" -i values.yaml

    - name: Commit and push changes
      run: |
        git config --global user.name 'github-actions'
        git config --global user.email 'github-actions@github.com'
        git add helm/nginx-app/values.yaml
        git commit -m "Update image tag to ${{ env.IMAGE_TAG }} [skip ci]" || exit 0
        git push